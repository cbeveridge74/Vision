<vn-css vn-id="vntaskdetail" vn-css-file="apps/visiontasks/vntaskdetail.css"></vn-css>
<div class="task-detail menu" ng-click="handleGeneralClick( $event );">
    
    <!--<div class="left">
    	<div class="sender-container">
    		<div class="sender">
    			<div class="icon">
    				<img ng-src="{{task.assigner.image}}">
    			</div>
    			<div class="name">
    				{{task.assigner.shortlabel}}
    			</div>
    		</div>
    		<div class="task-info">
    			<div class="sent">
    				Sent: {{task.sent | date:'dd-MMM-yyyy'}}
    			</div>
    			<div class="due">
    				Due:<span ng-hide="task.due == null"> {{task.due | date:'dd-MMM-yyyy'}}</span><span ng-show="task.due == null"> No due date</span>
    			</div>
    			<div class="urgency-container">
                    <div class="icon urgent fa fa-arrow-up" ng-show="task.urgency == 'Urgent'"></div>
                    <div class="icon fa fa-arrow-down" ng-show="task.urgency == 'Routine'"></div>
    				<div ng-class="task.urgency == 'Urgent' ? 'urgency urgent' : 'urgency'">{{task.urgency}}</div>
    			</div>
    		</div>
    	</div>
    	<div class="attachments">
    		<div class="patient-container">
    			<div class="icon fa fa-user"></div>
    			<div class="patient" ng-repeat="patient in task.attachments.selectedPatients">
    				<div>{{ patient.title }}</div>
    			</div>
    			<div class="none-label" ng-show="task.attachments.selectedPatients == null || task.attachments.selectedPatients.length < 1">No patients attached</div>
    		</div>
    		<div class="document-container">
    			<div class="icon fa fa-paperclip"></div>
    			<div class="document" ng-repeat="document in task.attachments.attachedDocs">
    				{{ document.label }}
    			</div>
    			<div class="none-label" ng-show="task.attachments.attachedDocs == null || task.attachments.attachedDocs.length < 1">No documents attached</div>
    		</div>
    		<div class="comment-container">
    			<div class="icon fa fa-comment-o"></div>
    			<div class="comment" ng-repeat="comment in task.attachments.comments">
    				{{ comment.label }}
    			</div>
    			<div class="none-label">No comments</div>
    		</div>
    		<div class="workflow-container" ng-click="handleWorkflowClick()">
    			{{ task.taskCount }} task<span ng-show="task.taskCount > 1">s</span> in this workflow
    		</div>
    	</div>
    </div>
    <div class="right">
    	<div class="subject">
    		{{task.tasksubjects.label}}
    	</div>
    	<div class="recipients-container">
    		<div class="label">To</div>
    		<div class="recipients" ng-repeat="assignee in task.assigneefull">
    			{{ assignee.title }}
    		</div>
    	</div>
    	<div class="content">

    	</div>
    	<div class="comments">
    		<div class="new">
    			new comments PLACEHOLDER
    		</div>
    		<div class="old">
    			old comments PLACEHOLDER
    		</div>
    	</div>
    </div>-->
</div>